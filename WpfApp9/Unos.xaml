<Window x:Class="WpfApp9.Unos"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp9"
        mc:Ignorable="d"
        Title="Unos" Height="450" Width="800">
    <UniformGrid Columns="2">
        <Label Content="Naziv: " MaxHeight="50"/>
        <TextBox Text="{Binding Ime, UpdateSourceTrigger=Explicit}" MaxHeight="50"/>
        <Label Content="Prezime: " MaxHeight="50"/>
        <TextBox MaxHeight="50" Text="{Binding Prezime,UpdateSourceTrigger=Explicit}"/>
        <Button Content="Otkazi" Click="Otkazi"/>
        <Button Content="OK" Click="Unoss"/>
    </UniformGrid>
</Window>
